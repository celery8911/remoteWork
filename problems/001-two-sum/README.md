# Two Sum (LC-001)

- **Approach:** Single-pass hash map storing value → index to find complements in O(n) time.
- **Complexities:** Time O(n), Space O(n).
- **Usage:** Run `ts-node solution.test.ts` (or compile with `tsc`) to verify sample cases.
