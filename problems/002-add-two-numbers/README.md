# Add Two Numbers (LC-002)

- **Approach:** Traverse both lists together, summing digits with carry; build result list on the fly.
- **Complexities:** Time O(max(m, n)), Space O(max(m, n)) for output list.
- **Usage:** Run `ts-node solution.test.ts` (or compile with `tsc`) to verify sample cases.
